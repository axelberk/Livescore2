import "./Header.css"

const Header = (params) => {
    return (
        <div className="header">
            <a href="/" className="home-button">Home</a>
        </div>
    )
}

export default Header